{
  "name": "bolt-bot",
  "version": "1.0.0",
  "description": "Experimental slack bot w/z ts",
  "main": "index.js",
  "scripts": {
    "local": "node_modules/.bin/nodemon dist/app.js",
    "start": "node dist/app.js",
    "build": "tsc -p .",
    "build:watch": "tsc -w -p .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@slack/bolt": "^3.4.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^15.12.4",
    "nodemon": "^2.0.7",
    "typescript": "^4.3.4"
  }
}
